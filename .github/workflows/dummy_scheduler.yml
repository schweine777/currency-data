name: Dummy Scheduler

on:
  schedule:
    - cron: '*/7 * * * *' 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Trigger Signal
        run: echo "스케줄이 작동했습니다. 메인 워크플로우를 트리거합니다."
